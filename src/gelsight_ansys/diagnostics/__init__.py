"""Licensed probes, coupons and benchmarks.

These commands answer questions about the solver and the hardware rather than
producing datasets: whether a licence checks out, what a contact law does on a
single element, how two numerical choices compare on the same short solve.
Their outputs are private diagnostics and never replace a validated example.
"""
